<!DOCTYPE html> 
    <html>
        <head>
            <title>Results of commercial detection</title>
            <link rel="stylesheet" type="text/css" href="/static/output/output.css">
            <script type="text/javascript" src="/static/output/output.js"></script>
        </head>
        <body> 
            <div class="container">
                <div class="video">
                    <br>
                    <br>
                    {% load staticfiles %}
                    <video id="mainvid" name="mainvid" autobuffer="autobuffer" controls="controls" style="text-align:center;">
                        <source src="/static/output/{{video_path}}" type="video/webm">
                        Your browser does not support HTML5 video.
                    </video>
                    <br>
                    <br>
                    <p><button onClick="save_changes();">SAVE CHANGES</button></p>
                    <p><button onClick="discard_changes();">DISCARD CHANGES</button></p>
                    
                </div>
                <div class="output">
                    <table cellspacing="10">
                        <tr align=left>
                            <th>Start</th>
                            <th>End</th>
                            <th>Name</th>
                            <th>Action</th>
                        </tr>
                        <form action="/output/update/" onsubmit="/update/" method="post">
                            {% csrf_token %}
                            {% for item in item_list %}
                            <tr>
                                <td>{{item.0}}<input type="hidden" name="start{{item.3}}" value="{{item.1}}"></input></td>
                                <td>{{item.1}}<input type="hidden" name="end{{item.3}}" value="{{item.1}}"></input></td>
                                <td><input type="text" name="name{{item.3}}" value="{{item.2}}"></input></td>
                                <td><button onClick="play({{item.3}});">Seek</button>
                            </tr>
                            {% endfor %}
                </div>
                <input type="submit" value="Save changes"/>
                </form>
            </div>
        </body> 
    </html>
